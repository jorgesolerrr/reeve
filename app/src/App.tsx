/**
 * The shell. Navigation between the five surfaces lands with the frontend
 * tickets (10-lld-frontend); until then this renders the one honest thing the
 * skeleton can claim — that the window opens.
 */
export function App() {
  return (
    <main>
      <h1>reeve</h1>
      <p>The floor is laid. Surfaces arrive with their tickets.</p>
    </main>
  );
}
